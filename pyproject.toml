[project]
name = "station-sdk-psa-firmware-upload"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "station-service-sdk>=2.1.0",
]
